# HidraNet — Exemplo de variáveis de ambiente
# Copie para .env e preencha (nunca commite o .env real)

# MQTT relay público (padrão — não é necessário alterar)
# MQTT_BROKER=wss://broker.emqx.io:8084/mqtt

# Porta do servidor local de apps
# CHAT_PORT=8090
